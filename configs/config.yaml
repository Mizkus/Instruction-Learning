defaults:
  - data: base
  - model: linear
  - training: base
  - logging: base
  - embedding_model: qwen3
  - instructions: topic
  - _self_

project:
  name: instruction-learning
  seed: 42

paths:
  project_root: ${oc.env:INSTRUCTION_LEARNING_PROJECT_ROOT,${oc.env:PWD}}
  data_root: ${oc.env:INSTRUCTION_LEARNING_DATA,${paths.project_root}/data}
  artifacts_root: ${oc.env:INSTRUCTION_LEARNING_ARTIFACTS,${paths.project_root}/artifacts}
  mlflow_root: ${oc.env:INSTRUCTION_LEARNING_MLFLOW,${paths.project_root}/mlruns}
